# Password used to encrypt the self-signed cert that signs the .zxp.
# Set any non-trivial value locally; CI reads this from a GitHub Actions secret.
# Falls back to "ae-ai-chat-dev" if unset (only acceptable for local dev builds).
ZXP_CERT_PASSWORD=
